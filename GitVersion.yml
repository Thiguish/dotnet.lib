mode: ContinuousDeployment
branches:
  main:
    regex: ^main$
    increment: Patch
  release:
    regex: ^release$
    increment: Minor
  pull-request:
    regex: ^pull/.*
    label: alpha-
    increment: none
    source-branches: ['main']
ignore:
  sha: []
