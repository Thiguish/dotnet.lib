mode: ContinuousDeployment
branches:
  main:
    regex: ^main$
    increment: Patch
    is-release-branch: true
  release:
    regex: ^release$
    increment: Minor
    is-release-branch: true
  pull-request:
    regex: ^pull/.*
    label: alpha-
    increment: none
    is-release-branch: false
    source-branches: ['main']
ignore:
  sha: []
